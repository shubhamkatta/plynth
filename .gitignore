__pycache__/
*.pyc
*.pyo
.venv/
.env
.env.local
*.sqlite
*.db
.coverage
htmlcov/
.pytest_cache/
.mypy_cache/
.ruff_cache/
dist/
build/
*.egg-info/
.DS_Store
.idea/
.vscode/

# Claude Code runtime artefacts — per-machine, not source of truth.
.claude/scheduled_tasks.lock
.claude/settings.local.json
skills-lock.json

# Secrets / certs — NEVER commit these. The platform expects them to live
# on the deployment host (in /opt/platform/certs/) or in your password
# manager, not in the repo.
*.pem
*.key
*.crt
*.p12
certs/

