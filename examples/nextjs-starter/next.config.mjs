/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  experimental: {
    // Server actions are enabled by default in Next 14 — kept here as a marker.
  },
};

export default nextConfig;
