"""plynth_cli — terminal admin/ops client for the Plynth platform.

Wraps the documented REST API (`/api/v1/*`) with Click subcommands. Mirrors
the surface area of `apps/admin-electron/` for terminal users.

Entrypoint: ``python -m plynth_cli`` (see ``plynth_cli/__main__.py``).
"""

__version__ = "0.1.0"
